# 实验二：使用 `millis()` 控制 LED 闪烁

## 一、实验目的
1. 掌握 ESP32 GPIO 输出控制方法  
2. 理解 `millis()` 函数的工作原理  
3. 使用非阻塞方式实现 1Hz LED 稳定闪烁  

---

## 二、实验器材
- ESP32 开发板 ×1  
- USB-C 数据线 ×1  
- 电脑（Arduino IDE） ×1  

---

## 三、实验原理

### 1. `millis()` 函数
`millis()` 返回系统启动以来运行的毫秒数。

优点：
- 不使用 `delay()`
- 程序可以同时执行其他任务
- 不会产生阻塞

### 2. 非阻塞 LED 控制思想
通过记录上一次状态切换的时间，判断是否到达间隔时间：